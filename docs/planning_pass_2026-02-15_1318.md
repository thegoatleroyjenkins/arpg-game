# Planning Pass — 2026-02-15 13:01 (America/Chicago)

Goal: Prioritize the next practical steps toward a full 3D ARPG with open-world-ready architecture.

## Top 3 Priorities

1. **Stabilize a 3D gameplay vertical slice (player + enemy + combat loop) in one test zone**
   - Why now: This is the fastest way to validate feel (weighty/responsive combat), camera, targeting, and encounter readability before scaling world complexity.
   - Deliverable: A single playable 3D test scene with spawn points, one melee skill, one ranged enemy behavior, damage feedback, and death/respawn flow.

2. **Introduce world-streaming boundaries and region contract (open-world foundation)**
   - Why now: Open-world readiness depends on chunk/region ownership, load/unload lifecycle, and deterministic handoff boundaries.
   - Deliverable: Region descriptor format + WorldStreamManager scaffold that can load/unload at least adjacent prototype regions around player position.

3. **Refactor gameplay into explicit service boundaries (combat/loot/ai/world) with event bus**
   - Why now: Prevents coupling as systems scale and enables future multiplayer/server authority options without rewrite.
   - Deliverable: Lightweight domain events and interfaces so world/AI can react to combat and loot without direct node dependencies.

## Immediate Next Implementation Task (do first)

**Implement `WorldStreamManager` scaffold + `RegionDescriptor` data resource and wire it into the current main scene.**

Minimum scope for this pass:
- Add a `RegionDescriptor` (id, bounds, scene_path, neighbor_ids).
- Add `WorldStreamManager` that tracks player region and emits load/unload requests.
- Support loading current + immediate neighbors; unload distant regions.
- Add debug overlay/log lines for active region set.

Success criteria:
- Walking across a boundary triggers predictable region transition logs.
- Only expected region scenes remain loaded.
- Existing prototype gameplay scene still runs.
